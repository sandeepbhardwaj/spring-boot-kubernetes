spring:
  cloud:
    kubernetes:
      reload:
        enabled: true
      config:
        enabled: true
        namespace: default
        name: message-producer
        sources:
          - namespace: default
          - name: messagek8config



